use std::foo;

use std::ds::{map as stdMap};

use std::{foo as bar};

mod std;

pub struct map<TKey, TValue> {}


mod foo::bar;

fn asdf() {}

struct tuples_test {
    a: (byte,byte);
    b: (byte,byte);
    c: (byte,rune,s32);
    d: (byte,rune,f32);
}


mod mycoollib::foobar;

struct Y<T> {
    y: T;
}

pub struct X<T> {
    a: Y<T>;
    b: fn(T)->T;
}

pub struct Z {
    pub x: X<s32>;
}


mod main;


pub enum Option<T>{
    Some(T),
    None
}

let x: bar::asdf;

struct ASDF<T> {
    x: Option<T>;
    y: mycoollib::foobar::Z;
}

struct BLABLABLA {
    x: ASDF<s32>;
}

pub interface Foo1<T> : Bar::foo, Bar2<T> where T : Asdf {
    pub fn foo(a: foo::bar, foo: s32);
    pub fn foo3() -> mut u8;
    mut pub fn foo2(a: u8, b: (u8, u8));
    pub fn bar<T>(t: T);
    pub fn qux<T1, T2>(@value: T1) where T1 : Someinterface<T2>;
    pub fn qux2<T1, T2>(@value: T1) -> T2 where T1 : Someinterface<T2>;

    pub get x() -> u8;
    pub mut get y() -> mut Bar<T>;

    pub set x(a: s32);
    pub set xy(y: u8);

    set x(@value: u8);
}

let bar: T;
mut let foo: u8;
let x: Foo<T>;

pub struct Foo2(x: s32, y: s32);

pub struct Foo3<T>  where T : Foobar {
    foo: s32;
    pub mut bar: s32;
}

pub struct Foo4<T> where T : Foobar {
    pub mut x: T;
    pub y: T;
} destructures into (x, y);

struct Bar {} destructures into (x, y);

alias bar<T> = (Foo2, std::map<Foo1<T>, T>) where T : numeric;

fn foo(){}

impl<T> Foo<T> on Bar<T> for Qux<T> {
    Bar(ref i: s32){}
    ~Bar(){}
    lol(){}
    fn foo(a: s32) -> Bar => ;
    fn foo2(b: string, t: T) {}
    mut get a() -> s32 {}
    set b(val: s32) => ;
}

fn foo<T>(mut t: T, ref x: s32) {
    whatever
}

fn foo(t: fn(s32)->s32) {

}



fn asdf(x: s32) {
    let f: fn(fn(s32)->s32) = foo(?);
    foo(x);
    foo<s32>(x);
}

fn foo(mapper: fn(s32)->s32) {

}

interface Foo5 {}
struct Foo6 {}


struct Foo7<T1,T2> {
    a: T1;
    b: T2;
}

alias Foo8<T2> = Foo7<T2, s32>;


mod foo;

pub struct X {}
pub struct Y {
    pub x: X;
}

pub enum Z {
    Foo(X),
}



mod bar;

struct asdf {
    x: ::foo::Y;
}

mod some::geometry::library;

pub struct Point2d<T> {
    x: T;
    y: T;
}


mod publicity;

pub struct B {}

pub struct A<T> {
    x: T;
}

pub struct Foo {
    pub a: A<B>;
}

struct somePrivateType{}

pub alias C<T> = A<T>;
pub alias D = C<somePrivateType>;


struct Vector<T> {
    ///
}

impl Vector<T> {
   pub fn first() -> T {}
    // ...
}


alias Pos32 = some::geometry::library::Point2d<s32>;