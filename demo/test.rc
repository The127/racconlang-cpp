use std::foo::;

mod main;

pub enum Option<T>{
    Some(T),
    None
}

pub interface Foo<T> : Bar, Bar2<T> where T : Asdf {
    pub get x -> u8;
    pub mut get y -> mut Bar<T>;

    set x(value: u8);
}